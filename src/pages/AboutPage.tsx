import React from 'react'
import { AboutWidget } from '../widgets/AboutWidget/AboutWidget'

const AboutPage: React.FC = () => {
  return <AboutWidget />
}

export default AboutPage  // 👈 ЭТА СТРОКА ОБЯЗАТЕЛЬНО ДОЛЖНА БЫТЬ